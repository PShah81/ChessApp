function Navbar(props){

    return (
        <div style={{display: "flex", flexDirection: "column"}}> 
            <button>Home</button>
            <button>Play</button>
            <button>About</button>
        </div>
    )
}

export default Navbar